project(AdditionalGeneralIgnorePrioritizationPlugin)

file(GLOB_RECURSE headers ./*.h)
aux_source_directory(${AdditionalGeneralIgnorePrioritizationPlugin_SOURCE_DIR} additional_general_ignore_prioritization_src)

add_library(additional_general_ignore_prioritization SHARED ${prioritization_plugin_header} ${headers} ${additional_general_ignore_prioritization_src})
target_link_libraries(additional_general_ignore_prioritization SoDA)
